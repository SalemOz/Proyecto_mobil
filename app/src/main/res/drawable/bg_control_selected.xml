<?xml version="1.0" encoding="utf-8"?>
<!-- Estado seleccionado de un control seleccionable (ambiente, tarjeta de oído). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/surface_card_selected" />
    <stroke
        android:width="2dp"
        android:color="@color/brand_primary" />
    <corners android:radius="@dimen/control_radius" />
</shape>
